FROM quay.io/fedora/fedora-toolbox:rawhide

LABEL com.github.containers.toolbox="true" \
      usage="This image is meant to be used with the toolbox or distrobox command" \
      summary="An example ContainerFile to demonstrate multiple image builds." \
      maintainer="mr@cory.farm"

# Copy the setup scripts and package list
COPY ../scripts/fedora.sh /
COPY ../scripts/toolbox-shims.sh /
COPY ../packages/fedora.packages /

# Run the setup scripts
RUN chmod +x fedora.sh toolbox-shims.sh && /fedora.sh
RUN rm /fedora.sh /toolbox-shims.sh /fedora.packages
