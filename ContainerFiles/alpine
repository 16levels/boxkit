FROM quay.io/toolbx-images/alpine-toolbox:edge

LABEL com.github.containers.toolbox="true" \
      usage="This image is meant to be used with the toolbox or distrobox command" \
      summary="A cloud-native terminal experience" \
      maintainer="mr@cory.farm"

# Copy the setup scripts and package list
COPY ../scripts/alpine.sh /
COPY ../scripts/toolbox-shims.sh /
COPY ../packages/alpine.packages /

# Run the setup scripts
RUN chmod +x alpine.sh toolbox-shims.sh && /alpine.sh
RUN rm /alpine.sh /toolbox-shims.sh /alpine.packages
